{
    "configurations": [
        {
            "type": "lldb-dap",
            "request": "launch",
            "name": "SandCoffin",
            "program": "${workspaceFolder}/out/build/linux-debug/bin/SandCoffin",
            "cwd": "${workspaceFolder}"
        }
    ],
}
